# Playwright_Proj

to install the Playwright two oprions

1. npm init -y
   npm install -D playwright and npx playwright install

2. npm init playwright@latest    // this will install all the setup at once 

# Folder structure 

node_modules
teests
package-lock.json
playwright.config.js


